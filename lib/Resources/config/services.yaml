services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false
    DawBed\ComponentBundle\:
        resource: '%bundle_dir%/*'
        exclude: '%bundle_dir%/{DependencyInjection}'
